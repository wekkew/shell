const manual = {
    nick : "a command to set a nickname on the server, requires one parameter: the nicknme (example: /nick mynickname)",
    online: "a command to list every online user on the server, optional parameter the listitem (example: /online - {gives a list with listmark '-' instead of the original '*'})",
    priv: "a command to send private message to an online client, requires two parameters the first is to whom it should send and from the second the message (example: /priv nickname random text what is the message)",
    yes: "a command answering the logi question (example: /yes)",
    all: "a command to list every valid command on the server, optinal parameter the listitem mark (example: /all -)",
    man: "a command to get the 'manual' for the requiested command, requires one parameter: the nam eof the wanted command (example: /man all {gives the manual for the all command})"
};
